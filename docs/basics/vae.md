# 变分自编码器

## 本章解决什么问题

普通自编码器给出的潜向量不一定能被当成概率状态。本章引入 VAE，讨论可采样潜空间及其在后续动力学中的位置。

## 学习目标

完成本章后，读者应该能够：

- [ ] 说明编码器为何输出分布而不是单点
- [ ] 写清重建项与正则项各自在约束什么
- [ ] 指出 posterior collapse 等失败模式需要被观察，而不是用名词跳过

## 前置知识

- [自编码器](autoencoder.md)；概率符号见 [数学基础](../appendices/math-foundations.md)。

## 从上一章遗留的问题开始

上一章：[自编码器](autoencoder.md)。

AE 给了压缩坐标，但还没有说明如何在这个空间里采样、插值，以及如何表达不确定的当前世界。

## 直观理解

把当前观测编码成“可能处于哪些潜状态”，而不是唯一的一个点。

## 数学形式

引入 \(q(z\mid x)\)、\(p(x\mid z)\) 与 KL 项的符号位置，不在占位页展开推导结论。

<!-- TODO：解释符号、公式和假设。不要在未核对前写入具体定理式结论。 -->

## 输入、输出与张量形状

| 名称 | 符号 | 示例形状 | 含义 |
|---|---|---:|---|
| 观测 | x | [B, ...] | 输入 |
| 均值 / 方差 | \mu, \sigma | [B, D] | 后验参数 |
| 采样潜变量 | z | [B, D] | 重参数化后的状态 |

## 模块结构

```mermaid
flowchart LR
    A["输入"] --> B["VAE"]
    B --> C["输出"]
```

## 配套实践

配套 Notebook 将在本章正文完成时一并接入。

!!! example "代码实践"
    本章网页只保留原理、公式、输入输出和图表。可执行实现将在对应章节的 Colab Notebook 中提供。

## 可视化与实验

<div class="viz-slot" data-viz="placeholder" data-viz-config='{"title":"变分自编码器","note":"本章动画或交互演示待接入。阅读不依赖这段可视化。"}'>
  <p data-viz-fallback>可视化占位：后续可在此展示 变分自编码器 的输入输出变化。</p>
</div>

<!-- TODO：图、动画或交互演示。实验记录请使用附录模板。 -->

## 常见误区

- 把 KL 项当成可以随意关掉的附加损失
- 把先验采样出来的图像质量直接当成状态估计质量

## 它在机器人世界模型中的位置

RSSM / Dreamer 一类世界模型会把类似的随机潜变量当成当前信念状态。本章只准备接口，不提前写控制结果。

## 本章小结

<!-- TODO：用三到五句收回本章缺口，不要复述未经验证的效果。 -->

本章把 **变分自编码器** 放进教程闭环中的对应位置，并留下一个旧方法无法覆盖的问题。

## 下一章

重建或生成观测并不是唯一学表示的办法。如果目标是可比较的特征，对比学习为什么会出现？

下一章：[对比学习](contrastive-learning.md)。
