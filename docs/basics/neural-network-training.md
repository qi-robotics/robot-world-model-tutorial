# 神经网络训练

## 本章解决什么问题

张量还不会自动变成表示。本章说明参数如何被损失驱动更新，以及训练循环里真正发生了什么。

## 学习目标

完成本章后，读者应该能够：

- [ ] 画出前向、损失、反向、更新的最小循环
- [ ] 区分训练目标、评价指标和部署时真正用到的输出
- [ ] 解释过拟合、欠拟合与不可复现实验的常见来源

## 前置知识

- [张量与数据](tensors-and-data.md)；建议对照 [PyTorch 基础](../appendices/pytorch-foundations.md)。

## 从上一章遗留的问题开始

上一章：[张量与数据](tensors-and-data.md)。

上一章只定义了数据的形状，还没有说明什么样的计算能被数据纠正。

## 直观理解

训练不是“让网络记住样本”，而是用可微目标把表示推到对后续任务有用的位置。

## 数学形式

引入损失 \(L(	heta)\) 与梯度更新的符号，不预设具体网络。

<!-- TODO：解释符号、公式和假设。不要在未核对前写入具体定理式结论。 -->

## 输入、输出与张量形状

| 名称 | 符号 | 示例形状 | 含义 |
|---|---|---:|---|
| 输入批次 | x | [B, ...] | 任意模态张量 |
| 监督信号 | y | [B, ...] | 标签、重建目标或动作 |
| 损失 | L | 标量 | 本次更新使用的目标 |

## 模块结构

```mermaid
flowchart LR
    A["输入"] --> B["训练循环"]
    B --> C["输出"]
```

## 配套实践

配套 Notebook 将在本章正文完成时一并接入。

!!! example "代码实践"
    本章网页只保留原理、公式、输入输出和图表。可执行实现将在对应章节的 Colab Notebook 中提供。

## 可视化与实验

<div class="viz-slot" data-viz="placeholder" data-viz-config='{"title":"神经网络训练","note":"本章动画或交互演示待接入。阅读不依赖这段可视化。"}'>
  <p data-viz-fallback>可视化占位：后续可在此展示 神经网络训练 的输入输出变化。</p>
</div>

<!-- TODO：图、动画或交互演示。实验记录请使用附录模板。 -->

## 常见误区

- 把训练损失下降等同于表示已经对机器人有用
- 在验证时仍使用会泄漏未来信息的数据管道

## 它在机器人世界模型中的位置

后面每一章的 AE、对比学习、扩散模型和 VLA，都只是在替换这个循环里的输入、输出和损失。

## 本章小结

<!-- TODO：用三到五句收回本章缺口，不要复述未经验证的效果。 -->

本章把 **神经网络训练** 放进教程闭环中的对应位置，并留下一个旧方法无法覆盖的问题。

## 下一章

通用训练循环有了，但图像仍然是一堆像素。怎样把它压成后续模块能用的视觉特征？

下一章：[视觉编码](vision-encoding.md)。
