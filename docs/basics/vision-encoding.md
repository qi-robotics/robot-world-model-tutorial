# 视觉编码

## 本章解决什么问题

相机给出的是像素网格。本章讨论如何把图像写成后续融合、预测和策略都能使用的视觉表征。

## 学习目标

完成本章后，读者应该能够：

- [ ] 说明视觉编码器的输入输出边界
- [ ] 区分骨干网络输出的空间特征和全局特征向量
- [ ] 指出分辨率、裁剪和视角变化会怎样破坏表示

## 前置知识

- [神经网络训练](neural-network-training.md)

## 从上一章遗留的问题开始

上一章：[神经网络训练](neural-network-training.md)。

训练循环可以优化任意映射，但还没有说明图像这种高维观测应该被映射到哪里。

## 直观理解

编码器的任务不是“看懂图片”，而是给出一个形状稳定、对后续模块友好的 \(z^{vis}\)。

## 数学形式

引入 \(z^{vis}_t = E_{vis}(o^{img}_t)\)。

<!-- TODO：解释符号、公式和假设。不要在未核对前写入具体定理式结论。 -->

## 输入、输出与张量形状

| 名称 | 符号 | 示例形状 | 含义 |
|---|---|---:|---|
| 图像 | o^{img}_t | [B, C, H, W] | 当前视觉观测 |
| 视觉特征 | z^{vis}_t | [B, N, D] 或 [B, D] | 空间 token 或全局向量 |

## 模块结构

```mermaid
flowchart LR
    A["输入"] --> B["视觉编码器"]
    B --> C["输出"]
```

## 最小实现

对应代码目录或接口待接入。当前仅保留可编译的函数签名。

```python
# TODO：最小可运行示例
# 本章模块：视觉编码器


def forward(*inputs):
    """返回本章定义的输出张量。"""
    raise NotImplementedError("《视觉编码》示例待补充")
```

## 可视化与实验

<div class="viz-slot" data-viz="placeholder" data-viz-config='{"title":"视觉编码","note":"本章动画或交互演示待接入。阅读不依赖这段可视化。"}'>
  <p data-viz-fallback>可视化占位：后续可在此展示 视觉编码 的输入输出变化。</p>
</div>

<!-- TODO：图、动画或交互演示。实验记录请使用附录模板。 -->

## 常见误区

- 默认全局池化后的向量已经保留了操作所需的空间信息
- 把预训练分类网络的输出直接当成机器人可用的场景状态

## 它在机器人世界模型中的位置

闭环图中“多模态编码器”的视觉分支。没有稳定的 \(z^{vis}\)，VLM 和世界模型都无法对齐当前场景。

## 本章小结

<!-- TODO：用三到五句收回本章缺口，不要复述未经验证的效果。 -->

本章把 **视觉编码** 放进教程闭环中的对应位置，并留下一个旧方法无法覆盖的问题。

## 下一章

视觉有了特征，语言指令仍是离散符号。怎样让一句话也进入同一套后续计算？

下一章：[语言编码](language-encoding.md)。
