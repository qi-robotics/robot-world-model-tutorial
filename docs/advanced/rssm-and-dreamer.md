# RSSM 与 Dreamer

## 本章解决什么问题

一类重要的世界模型用递推状态空间模型维护随机信念，并在想象中学习策略。本章梳理 RSSM / Dreamer 的模块切分。

## 学习目标

完成本章后，读者应该能够：

- [ ] 拆出确定性隐状态、随机隐状态、观测解码和动作条件转移
- [ ] 说明“在想象中训练策略”改变了数据流
- [ ] 标出与本教程闭环图的对应关系

## 前置知识

- [世界模型](world-model.md)、[变分自编码器](../basics/vae.md)

## 从上一章遗留的问题开始

上一章：[世界模型](world-model.md)。

世界模型接口已经定义，但还缺少一个被广泛使用的隐变量序列实现来对照。

## 直观理解

一边用真实观测纠正信念，一边在自己的动力学里做梦并改进策略。

## 数学形式

引入 RSSM 的状态分解符号，具体更新式待正文填写。

<!-- TODO：解释符号、公式和假设。不要在未核对前写入具体定理式结论。 -->

## 输入、输出与张量形状

| 名称 | 符号 | 示例形状 | 含义 |
|---|---|---:|---|
| 观测 | o_t | [B, ...] | 真实观测 |
| 动作 | a_t | [B, A] | 执行的动作 |
| 递推状态 | h_t, z_t | [B, D] | 确定性 / 随机状态 |

## 模块结构

```mermaid
flowchart LR
    A["输入"] --> B["RSSM / Dreamer"]
    B --> C["输出"]
```

## 最小实现

对应代码目录或接口待接入。当前仅保留可编译的函数签名。

```python
# TODO：最小可运行示例
# 本章模块：RSSM / Dreamer


def forward(*inputs):
    """返回本章定义的输出张量。"""
    raise NotImplementedError("《RSSM 与 Dreamer》示例待补充")
```

## 可视化与实验

<div class="viz-slot" data-viz="placeholder" data-viz-config='{"title":"RSSM 与 Dreamer","note":"本章动画或交互演示待接入。阅读不依赖这段可视化。"}'>
  <p data-viz-fallback>可视化占位：后续可在此展示 RSSM 与 Dreamer 的输入输出变化。</p>
</div>

<!-- TODO：图、动画或交互演示。实验记录请使用附录模板。 -->

## 常见误区

- Dreamer 这个名字本身已经说明了全部实现细节
- 想象中的奖励可以无检查地代替真机评价

## 它在机器人世界模型中的位置

这是“学习世界 + 在世界中规划或学策略”的经典路线。后面的 JEPA 路线会弱化像素重建，强化表示空间预测。

## 本章小结

<!-- TODO：用三到五句收回本章缺口，不要复述未经验证的效果。 -->

本章把 **RSSM 与 Dreamer** 放进教程闭环中的对应位置，并留下一个旧方法无法覆盖的问题。

## 下一章

如果预测目标从像素/重建转到表示空间，视频版 JEPA 会怎样改写世界预测？

下一章：[V-JEPA](v-jepa.md)。
