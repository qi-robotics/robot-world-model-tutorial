# Transformer

## 本章解决什么问题

要把注意力变成可训练、可扩展的序列模型，还需要位置信息、层结构和训练接口。本章进入 Transformer。

## 学习目标

完成本章后，读者应该能够：

- [ ] 画出一块 Transformer 的最小结构
- [ ] 说明自注意力与前馈层各自承担什么
- [ ] 写出序列长度如何进入计算代价

## 前置知识

- [Attention](attention.md)

## 从上一章遗留的问题开始

上一章：[Attention](attention.md)。

注意力给了选择机制，但还没有成为稳定的多层序列骨干。

## 直观理解

用同一套注意力块处理 token 序列：图像块、词、状态、动作都可以先变成 token 再送进去。

## 数学形式

引入残差、层归一化与位置编码的符号位置。

<!-- TODO：解释符号、公式和假设。不要在未核对前写入具体定理式结论。 -->

## 输入、输出与张量形状

| 名称 | 符号 | 示例形状 | 含义 |
|---|---|---:|---|
| token 序列 | X | [B, T, D] | 任意模态 token |
| 上下文序列 | H | [B, T, D] | Transformer 输出 |

## 模块结构

```mermaid
flowchart LR
    A["输入"] --> B["Transformer"]
    B --> C["输出"]
```

## 配套实践

配套 Notebook 将在本章正文完成时一并接入。

!!! example "代码实践"
    本章网页只保留原理、公式、输入输出和图表。可执行实现将在对应章节的 Colab Notebook 中提供。

## 可视化与实验

<div class="viz-slot" data-viz="placeholder" data-viz-config='{"title":"Transformer","note":"本章动画或交互演示待接入。阅读不依赖这段可视化。"}'>
  <p data-viz-fallback>可视化占位：后续可在此展示 Transformer 的输入输出变化。</p>
</div>

<!-- TODO：图、动画或交互演示。实验记录请使用附录模板。 -->

## 常见误区

- 把层数和参数量当成已经解决时间建模的证据
- 在需要严格因果的动作生成里误用双向注意力

## 它在机器人世界模型中的位置

它是后续 VLM、VLA、视频预测和规划模型最常见的骨干。世界模型不等于 Transformer，但很多世界模型会用它实现。

## 本章小结

<!-- TODO：用三到五句收回本章缺口，不要复述未经验证的效果。 -->

本章把 **Transformer** 放进教程闭环中的对应位置，并留下一个旧方法无法覆盖的问题。

## 下一章

现在能处理一种序列了。图像、语言和机器人状态同时出现时，怎样融合，而不是各说各话？

下一章：[多模态融合](multimodal-fusion.md)。
