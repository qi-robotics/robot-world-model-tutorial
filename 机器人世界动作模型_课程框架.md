# 《从编码到行动：机器人世界模型渐进式教程》课程规划

## 一、文档定位

本文档是后续逐章写作的总设计稿，不是正式教程正文。每章按照“承接问题—知识点—核心公式—图示/动画—Notebook 实践—下一章出口”规划，后续正文、Notebook、示例代码和网页内容均以此为依据。网页正文不嵌入 Python 实现，代码统一放在配套 Notebook 中。

教程始终回答一个问题：

> 机器人怎样从当前观测中理解世界和任务，生成合理动作，预测动作后果，并在真实闭环中持续选择、执行和纠错？

~~~text
现实世界
└── 观测世界
    └── 表示当前状态
        └── 理解任务目标
            └── 生成候选动作
                └── 预测动作后果
                    └── 评价并选择未来
                        └── 执行、反馈与纠错
~~~

| 部分 | 核心问题 | 阶段产物 |
|---|---|---|
| 基础篇 | 机器人怎样看懂当前世界 | 当前状态表示与第一个闭环策略 |
| 进阶篇 | 机器人怎样生成动作并预见变化 | Context、Action Model、World Model 与规划基础 |
| 高级篇 | 机器人怎样形成完整闭环智能系统 | VLA、World-Action Model 与真实机器人系统 |

### 编号说明

当前草案在第 38 章后直接进入第 42 章。本文暂时保留 39—41 为空号，作为未来扩展位，不创建对应章节。层级规划、长期记忆、主动感知和自我改进等内容分别并入第 34、37、38 章。若后续确定不再预留，可在正式创建页面前将第 42—47 章统一重编号为第 39—44 章。

## 二、贯穿全文的统一任务

主任务保持为：

> 桌面上有多个物体，用户说：“把红色杯子放进盒子里。”

随着课程推进，机器人依次获得以下能力：

1. 读取图像、语言、本体和触觉观测。
2. 识别目标物体、容器及其空间关系。
3. 把多模态信息组织为当前状态表示。
4. 生成接近、抓取、移动和放置动作。
5. 表达多条可能成功的动作轨迹。
6. 预测不同动作可能造成的未来。
7. 根据目标、价值和安全约束选择动作。
8. 执行后重新观测，并在偏差或失败时恢复。

各章可以使用更小的教学任务，但必须说明它对应主任务中的哪一段能力。

## 三、统一符号与系统接口

| 符号 | 含义 |
|---|---|
| \(s_t\) | 时刻 \(t\) 的真实环境状态，通常不可完全观测 |
| \(o_t\) | 机器人实际获得的多模态观测 |
| \(g\) | 语言、图像或状态形式的任务目标 |
| \(x_t\) | 已整理的结构化模型输入 |
| \(z_t\) | 模型形成的当前潜状态或上下文表示 |
| \(a_t\) | 单步机器人动作 |
| \(A_t=a_{t:t+H-1}\) | 长度为 \(H\) 的动作块或候选动作序列 |
| \(\tau\) | 一条完整轨迹或 episode |
| \(\pi_\theta\) | 动作策略或 Action Model |
| \(p_\phi\) | 动力学模型或 World Model |
| \(r_t,c_t,V,Q\) | 奖励、代价、状态价值与动作价值 |
| \(B,T,L,D,A,K\) | batch、时间、语言长度、特征维度、动作维度、候选数 |

整套教程最终形成四个核心接口：

$$
z_t=E_\psi(o_{\leq t},g)
$$

$$
A_t\sim\pi_\theta(A\mid z_t)
$$

$$
\hat Z_{t+1:t+H}\sim p_\phi(Z\mid z_t,A_t)
$$

$$
A_t^*=\arg\max_A U\!\left(\hat Z_{t+1:t+H},A,g\right)
$$

## 四、每章统一写作结构

1. 本章解决什么问题。
2. 学习目标与前置知识。
3. 从上一章遗留的问题开始。
4. 直观理解与贯穿任务。
5. 数学形式、符号与假设。
6. 输入、输出与张量形状。
7. 模块结构、训练目标和推理流程。
8. 实现接口、张量形状与计算步骤，不在正文展示 Python 代码。
9. 配套图示、动画或交互。
10. 配套 Notebook 实践。
11. 常见误区、失败案例和适用边界。
12. 它在完整机器人系统中的位置。
13. 本章小结与下一章矛盾。

每章不追求罗列全部算法，而必须讲清：

- 为什么上一章的方法不够；
- 新增了什么建模对象；
- 输入、输出、损失与推理过程是什么；
- 它解决机器人闭环中的哪一段问题；
- 它在什么情况下会失败。

### Notebook 代码层级

- **公式验证**：在 Notebook 中用 NumPy 手工实现，突出计算过程。
- **最小模块**：在 Notebook 中用 PyTorch 实现可训练网络。
- **小型实验**：使用人工数据或轻量公开数据，可在 CPU、免费 Colab 或 `.venv-colab` 运行。
- **系统实验**：放入 `examples/`，明确硬件、数据、依赖和运行时间。
- **网页正文**：只保留原理、公式、输入输出、计算步骤、图表和 Notebook 入口。

### 图示与动画

- 数据流、模块关系和闭环优先使用 Mermaid。
- 曲线、坐标、张量变化和几何关系优先使用自绘 SVG。
- 外部图片必须记录来源与许可。
- 动态过程只有在静态图难以表达时才使用动画。

动画占位统一使用：

~~~html
<div class="viz-slot" data-viz="chapter-animation-id"
     data-viz-config='{"title":"动画标题","note":"交互实现待接入"}'>
  <p data-viz-fallback>关闭脚本时仍能读懂的静态说明。</p>
</div>
~~~

---

# 第一部分　基础篇：机器人怎样看懂当前世界

基础篇从现实交互和训练机制出发，依次解决视觉、几何、语言、本体、触觉和动作的表示问题。阶段终点不是“认识很多编码器”，而是得到一个能够根据当前多模态观测执行简单任务的闭环策略。

## 单元一　从现实交互到学习问题

### 01　机器人眼中的世界是什么【已完成】

**承接与目标**

作为课程入口，建立机器人学习的数据对象和时间语义，让读者能够读懂一条真实轨迹。

**知识点规划**

- 环境、观测、状态、目标、动作、策略、动力学和 episode。
- 单步状态转移、完整轨迹和时间窗口。
- 开环与闭环执行。
- 多传感器同步、控制延迟与因果对应。
- 坐标系、单位、动作语义和数据契约。
- batch、time、channel、state、action 等张量维度。
- padding、mask、episode 级数据划分和未来信息泄漏。

**核心公式**

$$
\tau=(o_0,a_0,o_1,a_1,\ldots,a_{T-1},o_T)
$$

$$
a_t\sim\pi(a_t\mid o_{\leq t},g),\qquad
p(s_{t+1}\mid s_t,a_t)
$$

**图示与实践**

- 图示：观察—决策—行动闭环；观测、状态与内部表示；多传感器时间轴。
- Notebook 01-01：观察一条机器人轨迹。
- Notebook 01-02：认识机器人数据的张量形状。

**章节出口**

数据已经能够被正确解释，但数据本身不会产生模型，引出可学习函数和训练过程。

### 02　神经网络怎样学会一个任务【已完成】

**承接与目标**

将轨迹转化为监督学习任务，解释参数怎样从示范数据中获得输入到动作的映射。

**知识点规划**

- 从轨迹定义输入、监督目标和样本对应关系。
- 结构化观测与输入信息充分性。
- 线性层、隐藏层、激活函数和前向计算。
- 损失、梯度、反向传播、优化器、batch 与 epoch。
- 归一化、训练集、验证集和测试集。
- 欠拟合、过拟合、数据泄漏与泛化。
- 离线动作误差与闭环任务成功的区别。

**核心公式**

$$
\hat a=f_\theta(x),\qquad
\mathcal L_{\mathrm{MSE}}=\frac{1}{N}\sum_i\|\hat a^{(i)}-a^{(i)}\|_2^2
$$

$$
\theta\leftarrow\theta-\eta\nabla_\theta\mathcal L
$$

**图示与实践**

- 图示：非线性动作关系、前向计算、梯度更新、训练/验证曲线。
- Notebook 02-01：前向计算与激活函数。
- Notebook 02-02：手工训练两层网络并测试闭环泛化。

**章节出口**

本章依赖数据集直接提供物体和目标位置；下一章开始从原始图像获得特征。

## 单元二　不同观测怎样变成模型表示

### 03　视觉编码：从像素到视觉特征

**承接与目标**

相机只提供像素，动作模型需要能表达物体、位置和场景结构的视觉特征。

**知识点规划**

- 图像张量、通道、归一化、裁剪和数据增强。
- 局部感受野、卷积、步幅、填充、池化与分辨率变化。
- 层级特征、CNN、Residual Block 和特征金字塔。
- Patch embedding 与视觉 token，为 ViT 留接口。
- 全局向量、空间特征图和 patch token 的信息差异。
- 冻结编码器、微调编码器与从头训练。
- 分类特征可能丢失精确位置这一机器人特有问题。

**核心公式与形状**

$$
Y_{b,c_o,h,w}=\sum_{c_i,u,v}K_{c_o,c_i,u,v}X_{b,c_i,h+u,w+v}
$$

$$
z_t^{\mathrm{vis}}=E_{\mathrm{vis}}(I_t)
$$

- 输入图像：[B, 3, H, W]。
- 全局特征：[B, D]；空间 token：[B, N, D]。

**图示、动画与 Notebook**

- 动画：卷积核滑动、特征图尺寸变化、感受野扩大。
- SVG：全局向量与空间 token 保留信息的差异。
- NumPy 实现卷积和边缘检测。
- PyTorch 小型 CNN 预测人工桌面图像中的物体位置。

**章节出口**

视觉特征能说明“看到了什么”，但机器人还要把像素落到三维空间和机械臂坐标。

### 04　空间与几何：从二维图像到三维关系

**承接与目标**

二维像素位置不能直接成为机械臂目标，必须建立相机、世界和机器人之间的几何关系。

**知识点规划**

- 针孔相机模型、内参、外参和深度。
- 像素、相机、基座和世界坐标系。
- 齐次坐标、刚体变换和 \(SE(3)\)。
- 旋转矩阵、欧拉角、四元数和轴角。
- RGB-D、点云、体素与多视角融合。
- 检测、分割、关键点和姿态估计的接口差异。
- 遮挡、标定误差和深度噪声。
- 相对位置、可达区域和抓取位姿。

**核心公式与形状**

$$
\lambda
\begin{bmatrix}u\\v\\1\end{bmatrix}
=K[R\mid t]
\begin{bmatrix}X\\Y\\Z\\1\end{bmatrix}
$$

$$
{}^{B}p=T^{B}_{C}\,{}^{C}p
$$

- 深度图：[B, 1, H, W]；点云：[B, N, 3+C]；位姿：[B, 7] 或 [B, 4, 4]。

**图示、动画与 Notebook**

- 动画：像素反投影为三维点，相机坐标动态变换到基座坐标。
- Notebook：RGB-D 反投影点云；杯子位置坐标变换；标定误差敏感性。

**章节出口**

机器人能够表达物体与空间，却还不知道自然语言指定了哪个对象和什么目标。

### 05　语言编码：从文字到任务表示

**承接与目标**

把长度可变、表达多样的语言指令转化为可与视觉和动作连接的任务表示。

**知识点规划**

- 字符、词、子词和 tokenization。
- token id、embedding、位置编码、padding 与 mask。
- Bag-of-Words、静态词向量和上下文语言表示。
- 句子级向量与 token 级表示。
- 对象、属性、空间关系和动作动词。
- 同义表达、组合指令、否定、歧义和未见词。
- 预训练语言模型的冻结、适配和微调。
- 语言目标与可验证成功条件的区别。

**核心公式与形状**

$$
e_i=W_{\mathrm{emb}}[w_i]+p_i,\qquad
z_g=\operatorname{Pool}(Z^{\mathrm{lang}},m)
$$

- token id：[B, L]；mask：[B, L]；token 表示：[B, L, D]。

**图示、动画与 Notebook**

- 动画：文本切分为 token、embedding 和任务向量。
- 图示：同一场景在不同指令下关注不同目标。
- Notebook：简单 tokenizer、padding/mask；小型编码器识别对象、颜色和目标位置。

**章节出口**

图像和语言之外，机器人还必须知道自身姿态、接触状态和动作的物理含义。

### 06　机器人本体、触觉与动作接口

**承接与目标**

补齐外部视觉无法提供的机器人自身状态和接触信息，并明确动作数据契约。

**知识点规划**

- 关节位置、速度、力矩、末端位姿和夹爪状态。
- 触觉图像、力/力矩、接触和滑移信号。
- 关节空间与笛卡尔空间动作。
- 绝对动作、增量动作、速度、力矩和混合控制。
- 连续、离散与混合动作。
- 控制频率、延迟、限幅、归一化与反归一化。
- 正向运动学、逆运动学和学习策略的接口。
- 视觉、本体和触觉的互补性。

**核心公式与形状**

$$
x_{\mathrm{ee}}=f_{\mathrm{FK}}(q),\qquad
\dot x_{\mathrm{ee}}=J(q)\dot q
$$

$$
a_t=[\Delta x,\Delta y,\Delta z,\Delta r_x,\Delta r_y,\Delta r_z,g_t]
$$

- 本体：[B, T, P]；触觉/力：[B, T, F]；动作：[B, T, A]。

**图示、动画与 Notebook**

- 动画：关节变化如何改变末端位姿；接触信号如何改变动作。
- Notebook：二连杆正向运动学；统一 observation/action schema；动作尺度恢复。

**章节出口**

各模态已经可单独编码，但大量机器人观测没有人工标签，引出自监督表示学习。

## 单元三　怎样学到更有用的表示

### 07　重建式表示学习：AE、VAE 与 MAE

**承接与目标**

通过恢复输入或缺失部分，利用无标签数据学习压缩表示。

**知识点规划**

- 编码器、瓶颈、解码器和重建目标。
- 自编码器与压缩表示。
- 像素重建可能过度关注纹理和背景。
- VAE 的概率编码、重参数化和连续潜空间。
- 重建项与 KL 正则。
- MAE 的 patch masking、可见 token 和遮挡重建。
- 表示质量与重建质量不完全等价。
- 重建模型在预训练、压缩和异常检测中的用途。

**核心公式**

$$
z=E_\theta(x),\quad \hat x=D_\phi(z),\quad
\mathcal L_{\mathrm{AE}}=\|x-\hat x\|^2
$$

$$
\mathcal L_{\mathrm{VAE}}
=-\mathbb E_{q_\theta(z\mid x)}[\log p_\phi(x\mid z)]
+\beta D_{\mathrm{KL}}(q_\theta(z\mid x)\|p(z))
$$

**图示、动画与 Notebook**

- 动画：瓶颈压缩、VAE 采样、MAE 遮挡恢复。
- Notebook：小型 AE；二维 VAE 潜空间插值；重建质量与位置预测对比。

**章节出口**

重建要求保留所有可恢复细节，机器人更需要任务相关、可比较和可预测的信息。

### 08　对比、对齐与预测式表示：Contrastive、CLIP 与 JEPA

**承接与目标**

学习忽略无关变化、保留语义对应和可预测结构的表示。

**知识点规划**

- 正负样本、数据增强、相似度与温度。
- InfoNCE 和 batch 内负样本。
- 表征坍缩与停止梯度、动量编码器、方差正则。
- 图像—文本双塔与 CLIP 式对齐。
- 检索、开放词汇语义和零样本能力。
- masked prediction 与 joint-embedding prediction。
- JEPA 的 context encoder、target encoder 和 predictor。
- 图像级与视频级预测表示。
- 语义对齐不等于保留几何和接触信息。

**核心公式**

$$
\mathcal L_i=-\log
\frac{\exp(\operatorname{sim}(z_i,z_i^+)/\tau)}
{\sum_j\exp(\operatorname{sim}(z_i,z_j)/\tau)}
$$

$$
\mathcal L_{\mathrm{JEPA}}
=\|P_\theta(E_c(x_{\mathrm{visible}}))-{\rm sg}(E_t(x_{\mathrm{target}}))\|_2^2
$$

**图示、动画与 Notebook**

- 动画：嵌入空间拉近/推远；JEPA 从上下文预测目标表示。
- Notebook：二维对比学习；图文相似度矩阵；最小 JEPA 与坍缩观察。

**章节出口**

每种模态都有表示，下一步要把它们组织成同一个当前状态。

### 09　多模态融合：构造机器人当前状态

**承接与目标**

融合长度、尺度、频率和可信度不同的视觉、语言、本体和触觉表示。

**知识点规划**

- early、intermediate 与 late fusion。
- 拼接、求和、门控和条件调制。
- 全局向量融合与 token 序列融合。
- 模态类型、位置、时间和缺失模态 mask。
- 语言如何条件化视觉与动作。
- 多频率传感器与模态 dropout。
- 共享空间与模态特有信息。
- \(z_t\) 与真实状态 \(s_t\) 的区别。
- 简单融合的边界，为 Attention 留出动机。

**核心公式**

$$
z_t=\phi(E_v(I_t),E_l(g),E_p(q_t),E_f(f_t))
$$

$$
z_t=\sum_m\alpha_m z_t^{(m)},\qquad
\alpha=\operatorname{softmax}(G(z^{(1)},\ldots,z^{(M)}))
$$

**图示、动画与 Notebook**

- 图示：四模态编码与融合；遮挡时视觉权重下降、触觉权重上升。
- Notebook：拼接、门控和 FiLM；模态随机缺失实验；输出统一 [B, D] 状态。

**章节出口**

统一状态已经形成，但还没有从示范中学习真正的动作策略。

## 单元四　让机器人第一次根据观测行动

### 10　行为克隆：第一个机器人策略

**承接与目标**

从专家轨迹训练一个根据观测和目标直接预测动作的策略。

**知识点规划**

- 模仿学习与监督学习。
- 状态—动作数据集和专家占用分布。
- 连续回归、离散分类和混合动作损失。
- 多任务条件策略 \(\pi(a\mid o,g)\)。
- teacher forcing、协变量偏移和复合误差。
- DAgger 的交互式数据修正思想。
- 动作归一化、维度加权和安全限幅。
- 离线误差、成功率、碰撞率和轨迹平滑度。

**核心公式**

$$
\theta^*=\arg\min_\theta
\mathbb E_{\mathcal D}[-\log\pi_\theta(a\mid o,g)]
$$

$$
\mathcal L_{\mathrm{BC}}=\|\pi_\theta(o,g)-a\|_2^2
$$

**图示、动画与 Notebook**

- 动画：专家分布与策略偏离；单步小误差沿轨迹累积。
- Notebook：二维 reaching 行为克隆；比较不同输入；闭环统计复合误差。

**章节出口**

单帧策略无法判断遮挡、速度、过去动作和任务阶段，引出部分可观测性。

### 11　闭环执行、误差累积与部分可观测性

**承接与目标**

解释为什么单帧状态和开环动作不足，并为时间、记忆和序列模型建立问题。

**知识点规划**

- 开环、闭环与 receding horizon。
- Markov 状态和部分可观测过程。
- 历史观测、历史动作和内部 belief。
- 遮挡、速度、接触和阶段为什么需要记忆。
- 观测噪声、执行误差、延迟和反馈频率。
- 错误检测、重新观测和最小恢复动作。
- 状态估计与策略记忆的区别。

**核心公式**

$$
b_t(s)=p(s_t=s\mid o_{\leq t},a_{<t})
$$

$$
a_t\sim\pi(a_t\mid o_{\leq t},a_{<t},g)
$$

**图示、动画与 Notebook**

- 动画：相同单帧、不同运动方向；开环与闭环；控制延迟时间线。
- Notebook：需要历史判断方向的 toy dataset；固定窗口模型；执行噪声对比。

**阶段出口**

基础篇完成“看懂当前世界并做出第一个动作”。进阶篇开始组织历史、表达多种动作未来并预测动作后果。

---

# 第二部分　进阶篇：机器人怎样生成动作并预见变化

进阶篇围绕三个模型展开：Context Model 组织历史，Action Model 生成动作分布，World Model 预测动作后果。阶段终点是能够提出候选动作、想象未来并进行简单规划的模型智能体。

## 单元一　从单帧状态到历史上下文

### 12　时间、记忆与序列建模：RNN、LSTM、TCN 与状态空间模型

**承接与目标**

将可变长度历史压缩成包含运动趋势、接触和任务阶段的记忆。

**知识点规划**

- 序列、因果性、滑动窗口和时间 mask。
- RNN 隐状态、共享参数和时间展开。
- 梯度消失、爆炸和截断反向传播。
- LSTM/GRU 的门控记忆。
- TCN 的因果卷积和膨胀感受野。
- 线性状态空间模型的状态更新与输出。
- 选择性状态空间模型的直觉和边界。
- 时间编码、异步传感器和长短期依赖。

**核心公式**

$$
h_t=f_\theta(h_{t-1},x_t),\qquad y_t=g_\theta(h_t)
$$

$$
h_{t+1}=Ah_t+Bx_t,\qquad y_t=Ch_t+Dx_t
$$

**图示、动画与 Notebook**

- 动画：隐状态时间展开；RNN、TCN、SSM 信息路径。
- Notebook：GRU 估计运动方向；TCN 判断接触阶段；可视化梯度范数。

**章节出口**

固定规则压缩历史，难以根据当前问题选择任意位置的信息，引出 Attention。

### 13　Attention：从大量信息中读取相关内容

**承接与目标**

让模型根据当前查询，从视觉区域、语言 token 和历史时刻中动态读取信息。

**知识点规划**

- Query、Key、Value。
- 点积相似度、缩放和 Softmax。
- Self-Attention 与 Cross-Attention。
- causal mask、padding mask 和未来泄漏。
- 多头注意力。
- 视觉—语言、当前—历史的对应。
- 权重不等于因果解释。
- 序列长度和计算复杂度。

**核心公式**

$$
\operatorname{Attention}(Q,K,V)
=\operatorname{softmax}\left(\frac{QK^\top}{\sqrt{d_k}}+M\right)V
$$

**形状**

- \(Q:[B,N_q,D]\)，\(K,V:[B,N_k,D]\)。
- 权重：[B, H, Nq, Nk]；输出：[B, Nq, D]。

**图示、动画与 Notebook**

- 动画：查询变化时注意力热图移动；causal mask；词语对齐视觉 patch。
- NumPy 手工实现 attention；可视化权重；构造未来泄漏反例。

**章节出口**

Attention 是一次读取操作，还需要残差、归一化、前馈网络和层叠结构。

### 14　Transformer：构造统一序列模型

**承接与目标**

将注意力组成可扩展、可训练的上下文模型。

**知识点规划**

- MHA、MLP、Residual 和 LayerNorm。
- Pre-Norm 与 Post-Norm。
- 一维、二维和时间位置编码。
- Encoder、Decoder 与 Encoder-Decoder。
- 自回归解码和 KV cache。
- ViT 的 patch token。
- token 压缩、序列长度和计算代价。
- 训练并行与推理自回归的区别。

**核心公式**

$$
X'=X+\operatorname{MHA}(\operatorname{LN}(X))
$$

$$
Y=X'+\operatorname{MLP}(\operatorname{LN}(X'))
$$

**图示、动画与 Notebook**

- 图示：Transformer Block；多模态 token 与位置编码。
- Notebook：最小 Transformer Encoder；从历史状态预测动作；causal 对照。

**章节出口**

Transformer 能处理通用 token，但还要决定机器人各模态怎样进入序列、输出什么。

### 15　多模态 Context Model：融合视觉、语言、状态与历史

**承接与目标**

把基础篇的模态编码器和本单元的序列模型组合成任务条件上下文。

**知识点规划**

- modality、time、camera 和 embodiment token。
- 单流、双流、Cross-Attention 和 latent bottleneck。
- 历史图像、本体、触觉、动作和语言的序列组织。
- CLS、query、spatial 和 action query。
- 多相机、缺失模态和模态 dropout。
- 冻结、adapter、LoRA 与端到端微调。
- 上下文长度、缓存和实时成本。
- 任务相关语义、几何和动态线索。

**核心公式**

$$
Z_t=\operatorname{Transformer}
\left[Z_{t-H:t}^{\mathrm{vis}},
Z_{t-H:t}^{\mathrm{prop}},
Z_{t-H:t}^{\mathrm{touch}},
Z^{\mathrm{lang}},A_{t-H:t-1}\right]
$$

**图示、动画与 Notebook**

- 图示：多模态 token 布局；语言查询视觉和历史；latent bottleneck。
- Notebook：构造类型、时间、mask；小型多模态 Transformer；融合方式对比。

**章节出口**

Context 已建立，下一单元研究动作如何表示、一次生成一步还是一段。

## 单元二　从单步动作到动作序列

### 16　动作空间、动作 Token 与 Action Chunk

**承接与目标**

明确动作空间、控制频率和预测跨度对学习与执行的影响。

**知识点规划**

- 关节、末端、速度、力矩和混合动作。
- 绝对动作与增量动作。
- 连续动作离散化、tokenization 和反量化。
- 单步动作与 Action Chunk。
- observation、prediction、execution horizon。
- overlapping chunk、temporal ensemble 和 receding horizon。
- 动作平滑、延迟和 chunk 边界。
- ACT 的条件 VAE 和 action query。
- 不同本体的动作语义。

**核心公式**

$$
A_t=[a_t,\ldots,a_{t+H-1}]\in\mathbb R^{B\times H\times A}
$$

$$
\tilde a_{t+k}=\sum_jw_j\hat a_{t+k}^{(j)}
$$

**图示、动画与 Notebook**

- 动画：三种 horizon；重叠 chunk；连续动作量化。
- Notebook：轨迹切分 chunks；temporal ensemble；单步/开环/滚动执行对比。

**章节出口**

同一任务常有多条合理路径，确定性回归可能产生危险的平均动作。

### 17　多峰动作分布：MDN、CVAE 与自回归策略

**承接与目标**

显式表达同一上下文下多种合理动作模式。

**知识点规划**

- aleatoric uncertainty 与多峰动作。
- Gaussian Policy 和 Mixture Density Network。
- CVAE 的 latent、prior、posterior 和 KL。
- 自回归动作因子分解。
- teacher forcing、exposure bias 和采样温度。
- likelihood、diversity 与任务成功率。
- 连续高维动作中的表达和推理限制。

**核心公式**

$$
p(a\mid z)=\sum_{k=1}^{K}\pi_k(z)\mathcal N(a;\mu_k(z),\Sigma_k(z))
$$

$$
p(A\mid z)=\prod_{h=0}^{H-1}p(a_{t+h}\mid a_{t:t+h-1},z)
$$

**图示、动画与 Notebook**

- 动画：双峰示范与 MSE 平均；CVAE 潜变量；自回归误差传播。
- Notebook：二维绕障 MDN；CVAE 多轨迹采样；三类策略对比。

**章节出口**

现有方法在表达能力或训练稳定性上受限，引出从噪声恢复轨迹的 Diffusion。

### 18　Diffusion：从噪声到连续动作分布

**承接与目标**

理解能够表达高维、多峰连续分布的条件去噪生成过程。

**知识点规划**

- 数据分布、噪声分布和逐步加噪。
- forward process 与 reverse denoising。
- epsilon、x0 和 velocity 参数化。
- 时间步 embedding 和条件输入。
- DDPM、DDIM 和采样步数。
- classifier-free guidance 的条件控制。
- 训练稳定性与推理迭代成本。
- 从图像生成迁移到动作序列。

**核心公式**

$$
x_t=\sqrt{\bar\alpha_t}x_0+\sqrt{1-\bar\alpha_t}\epsilon
$$

$$
\mathcal L_{\mathrm{diff}}
=\mathbb E\|\epsilon-\epsilon_\theta(x_t,t,c)\|_2^2
$$

**图示、动画与 Notebook**

- 动画：二维动作分布加噪/去噪；不同采样步；条件引导。
- Notebook：条件二维 diffusion；逐步可视化；步数和随机种子实验。

**章节出口**

Diffusion 通过离散多步去噪生成动作，下一章从连续运输和 ODE 角度理解 Flow Matching。

### 19　Flow Matching：学习动作生成的连续流

**承接与目标**

学习把简单分布连续运输到动作分布的速度场。

**知识点规划**

- source、target 和 probability path。
- 线性插值和条件速度场。
- Conditional Flow Matching。
- Neural ODE 与数值积分。
- Flow Matching、Rectified Flow 与 Diffusion 的联系。
- ODE/SDE 推理、步数、曲率和误差。
- 条件动作生成、warm start 和少步推理。

**核心公式**

$$
x_t=(1-t)x_0+t x_1,\qquad u_t=x_1-x_0
$$

$$
\mathcal L_{\mathrm{FM}}
=\mathbb E\|v_\theta(x_t,t,c)-u_t\|_2^2,\qquad
\frac{dx}{dt}=v_\theta(x,t,c)
$$

**图示、动画与 Notebook**

- 动画：点沿速度场运输；路径曲率与积分步数；Diffusion/Flow 对照。
- Notebook：二维 Flow Matching；Euler/RK 积分；与 Diffusion 对比。

**章节出口**

已经分别理解动作块、Diffusion 和 Flow，下一章把它们放回闭环机器人策略。

### 20　生成式机器人策略：ACT、Diffusion Policy 与 Flow Policy

**承接与目标**

把生成模型接入机器人上下文、动作数据契约和滚动执行。

**知识点规划**

- 视觉、语言、本体和历史条件。
- ACT：CVAE、action query、chunk 和 temporal ensemble。
- Diffusion Policy：动作去噪与 receding horizon。
- Flow Policy：条件速度场与轨迹积分。
- Transformer、U-Net 和 MLP 动作 backbone。
- open-loop chunk 与 closed-loop replanning。
- 动作连续性、抖动、延迟和异步推理。
- 多样性、成功率、频率和成本的权衡。
- 统一 \(p(A_t\mid z_t)\) 接口。

**核心公式**

$$
A_t\sim\pi_\theta(A\mid z_t),\qquad z_t=E(o_{\leq t},g)
$$

**图示、动画与 Notebook**

- 图示：三类策略结构；异步执行时间线；候选动作采样。
- Notebook：统一策略接口；相同 toy task 对比成功率、平滑度和延迟。

**章节出口**

Action Model 能提出动作，却不知道动作执行后世界会怎样变化。

## 单元三　从生成动作到预测动作后果

### 21　动作条件动力学：预测下一步变化

**承接与目标**

区分“应该做什么”的策略与“做了以后会怎样”的动力学。

**知识点规划**

- forward dynamics 与 inverse dynamics。
- 状态、观测和潜状态转移。
- deterministic 与 stochastic dynamics。
- 绝对状态与残差预测。
- 单步监督、动作对齐和 teacher forcing。
- 接触、不连续动力学和多结果未来。
- inverse model 的表示约束。
- model bias 和训练分布之外的动作。

**核心公式**

$$
\hat s_{t+1}=f_\phi(s_t,a_t),\qquad
\mathcal L_{\mathrm{dyn}}=\|s_{t+1}-f_\phi(s_t,a_t)\|_2^2
$$

**图示、动画与 Notebook**

- 动画：同状态不同动作；forward/inverse；接触边界多未来。
- Notebook：二维推块动力学；绝对/增量预测；未见动作外推。

**章节出口**

结构化状态易预测，但真实机器人主要拥有图像，引出像素、视频和 latent 预测。

### 22　像素、视频与潜空间未来预测

**承接与目标**

比较不同未来表示保留的信息、训练目标和计算成本。

**知识点规划**

- next-frame 与 action-conditioned video prediction。
- 像素 MSE 的模糊和多峰未来。
- 自回归视频、latent video 和生成式视频。
- 编码—动力学—解码结构。
- perceptual、feature 与 pixel loss。
- 视觉逼真度和规划可用性的区别。
- 对象中心、scene graph 和 slot 表示。
- 动作、相机运动和背景变化的解耦。

**核心公式**

$$
\hat I_{t+1}=D_\psi(f_\phi(E_\psi(I_t),a_t))
$$

$$
\hat z_{t+1}=f_\phi(z_t,a_t)
$$

**图示、动画与 Notebook**

- 图示：像素/latent 路线；多峰平均模糊；动作条件 rollout。
- Notebook：Moving Shapes 下一帧；不同损失；多动作 latent rollout。

**章节出口**

单独 encoder 和 transition 不足以维护长期 belief 与随机性，引出潜状态世界模型。

### 23　潜状态世界模型：State Space、RSSM、Dreamer 与 JEPA

**承接与目标**

把历史压入 belief，在 latent 中预测未来，并处理不可直接观测的随机因素。

**知识点规划**

- prior、posterior、transition 和 observation model。
- deterministic memory 与 stochastic state。
- RSSM 的 recurrent state 和 latent。
- reconstruction、reward、continue 和 KL。
- Dreamer 的 latent imagination 和 actor-critic 接口。
- JEPA 式未来表示预测。
- target encoder、predictor 和 collapse 防护。
- 生成式与预测式 world model 的差异。
- 验证 latent 是否保留控制相关信息。

**核心公式**

$$
h_t=f(h_{t-1},z_{t-1},a_{t-1})
$$

$$
q(z_t\mid h_t,o_t),\qquad p(z_t\mid h_t)
$$

$$
\mathcal L_{\mathrm{WM}}
=\mathcal L_{\mathrm{obs}}+\mathcal L_{\mathrm{reward}}
+\beta D_{\mathrm{KL}}(q\|p)
$$

**图示、动画与 Notebook**

- 图示：RSSM prior/posterior；观测更新与 imagined rollout；JEPA 对照。
- Notebook：简化 stochastic dynamics；posterior correction；latent probe。

**章节出口**

World Model 能想象未来，但多步误差会累积，模型也未必知道何时不可靠。

### 24　多步 Rollout、不确定性与误差累积

**承接与目标**

理解单步准确为何不保证长期可信，并让规划感知模型不确定性。

**知识点规划**

- one-step training 与 open-loop rollout。
- teacher forcing/free-running mismatch。
- compounding error 与 model bias。
- aleatoric 与 epistemic uncertainty。
- probabilistic dynamics、ensemble 和校准。
- 多未来采样、置信区间和 OOD 检测。
- scheduled sampling、multi-step loss 和 consistency。

**核心公式**

$$
\hat z_{t+h+1}=f_\phi(\hat z_{t+h},a_{t+h})
$$

$$
\mathcal L_H=\sum_{h=1}^{H}w_h\|\hat z_{t+h}-z_{t+h}\|^2
$$

**图示、动画与 Notebook**

- 动画：预测带随 horizon 变宽；两类不确定性；ensemble 分歧。
- Notebook：多步 rollout；ensemble 置信带；单步/多步训练对比。

**章节出口**

可以生成多个未来，但仍缺少“哪个未来更好”的评价标准。

## 单元四　从预测未来到选择动作

### 25　目标、奖励、代价与价值函数

**承接与目标**

把任务进展、安全和效率转化为可比较的未来评价。

**知识点规划**

- goal、success detector、reward 和 cost。
- 稀疏/稠密奖励和 reward shaping。
- 终止条件、折扣回报和 horizon。
- value、Q-value 和 advantage。
- Bellman consistency。
- 语言目标、目标图像和 learned reward。
- 成功、碰撞、时间、能耗和平滑的多目标权衡。
- reward hacking 和风险敏感效用。

**核心公式**

$$
G_t=\sum_{k=0}^{\infty}\gamma^k r_{t+k}
$$

$$
Q^\pi(s,a)=r(s,a)+\gamma\mathbb E_{s'}[V^\pi(s')]
$$

**图示、动画与 Notebook**

- 图示：不同安全代价轨迹；稀疏/稠密信号；未来评分。
- Notebook：设计目标、碰撞和成功奖励；检查奖励捷径；训练 success classifier。

**章节出口**

能够评价未来后，下一章使用 World Model 搜索最优候选动作。

### 26　规划与模型预测控制：MPC、CEM 与 Imagination

**承接与目标**

在真实执行前生成、预测和比较多条动作序列。

**知识点规划**

- planning 与 policy。
- random shooting、gradient planning 和 CEM。
- CEM 的采样、精英筛选和分布更新。
- MPC 的滚动优化。
- state、pixel 与 latent planning。
- terminal value、有限 horizon 和 uncertainty penalty。
- warm start、采样预算和实时性。
- policy proposal + planner refinement。

**核心公式**

$$
A^*=\arg\max_A
\mathbb E_{p_\phi}\left[
\sum_{h=0}^{H-1}\gamma^h r(\hat z_{t+h},a_{t+h})
+\gamma^H V(\hat z_{t+H})\right]
$$

**图示、动画与 Notebook**

- 动画：候选未来评分；CEM 分布聚焦；MPC 重新规划。
- Notebook：random shooting；CEM；模型偏差和不确定性惩罚。

**章节出口**

规划每一步搜索成本较高，策略还可利用成功、失败和离线数据继续改进。

### 27　策略改进：强化学习、离线强化学习与模仿学习

**承接与目标**

统一理解模仿、奖励驱动策略优化和基于模型规划。

**知识点规划**

- imitation、RL 与 planning 的关系。
- on/off-policy、model-free/model-based。
- policy gradient、actor-critic 和 advantage。
- replay、bootstrapping 和 target network。
- offline RL 的分布外动作问题。
- conservative value、behavior regularization 和 advantage-weighted regression。
- demonstration + RL、offline-to-online 和安全探索。
- 生成式策略的强化学习接口。

**核心公式**

$$
J(\theta)=\mathbb E_{\tau\sim\pi_\theta}\left[\sum_t\gamma^tr_t\right]
$$

$$
\nabla_\theta J
=\mathbb E[\nabla_\theta\log\pi_\theta(a_t\mid s_t)\hat A_t]
$$

**图示、动画与 Notebook**

- 图示：BC、offline RL、online RL、planning 的数据闭环；OOD 动作。
- Notebook：BC 与 advantage-weighted 更新；离线 Q 过估计；有限在线改进。

**阶段出口**

进阶篇具备 Context、Action Model、World Model、评价和规划。高级篇把它们扩展到大规模视觉语言预训练和真实机器人系统。

---

# 第三部分　高级篇：机器人怎样形成完整的闭环智能系统

高级篇不再孤立介绍算法，而是研究大规模预训练模型、机器人数据和系统组件怎样协同。重点从“某个模型能否训练”转向“整个系统在新任务、新场景和真实硬件上能否持续工作”。

## 单元一　从多模态理解到语言驱动行动

### 28　视觉语言模型：VLM 如何理解机器人场景

**承接与目标**

从图文对齐进一步走向问答、组合指令和结构化场景理解。

**知识点规划**

- vision encoder、projector/resampler、LLM 和 decoder。
- 图像 token 进入语言模型的方式。
- contrastive pretraining、captioning 和 instruction tuning。
- VQA、描述、开放词汇识别和多轮指令。
- frozen backbone、adapter、LoRA 和全量微调。
- prompt 和 structured output。
- hallucination、language prior 和视觉细节丢失。
- VLM 语义能力与低层控制精度的边界。

**核心公式**

$$
p(y\mid I,g)=\prod_{i=1}^{L}p(y_i\mid y_{<i},E_v(I),g)
$$

**图示、动画与 Notebook**

- 图示：Vision Encoder—Projector—LLM；联合 token 序列；幻觉反例。
- Notebook：轻量 VLM 场景问答；结构化 JSON 输出；颜色、计数和关系反例。

**章节出口**

VLM 能谈论场景，但还需把语言精确落到物体、位置和可操作区域。

### 29　Grounding、Affordance 与三维空间推理

**承接与目标**

把“红色杯子”和“盒子里”落实为实例、区域、三维位置和可执行约束。

**知识点规划**

- referring expression comprehension。
- bounding box、mask、keypoint 和 object token。
- open-vocabulary detection/segmentation。
- 2D 到 3D grounding。
- scene graph、对象关系和空间谓词。
- affordance 与可抓取、可放置、可推动区域。
- grasp pose、pre-grasp 和 collision-free region。
- 语义正确但几何不可执行的失败。

**核心公式**

$$
\hat r=\arg\max_r p(r\mid I,g)
$$

$$
p(a\ \text{feasible}\mid o,g)
\quad\text{或}\quad \mathcal A_{\mathrm{safe}}(o,g)
$$

**图示、动画与 Notebook**

- 动画：语言短语到 2D mask、3D 点集和抓取位姿。
- Notebook：合成 grounding；mask+depth 反投影；抓取候选几何过滤。

**章节出口**

模型能够理解目标和操作区域，下一章把高层理解连接到动作生成。

### 30　视觉语言动作模型：VLA 的完整结构

**承接与目标**

把 VLM 的视觉语言先验与机器人动作数据连接成端到端语言条件策略。

**知识点规划**

- VLA 的输入、输出和训练数据。
- VLM backbone、robot adapter、proprio encoder 和 action head。
- action-as-token 与 continuous action expert。
- 自回归、并行 chunk 和生成式动作。
- 预训练、机器人微调和多任务训练。
- 冻结、部分微调、LoRA 与 action expert。
- 历史、多相机、本体和语言条件。
- embodiment/dataset id 和 action normalization。
- VLM、VLA、planner 和 world model 的边界。

**核心公式**

$$
A_t\sim\pi_\theta(A\mid I_{\leq t},q_{\leq t},g)
$$

$$
\mathcal L_{\mathrm{VLA}}
=\lambda_{\mathrm{act}}\mathcal L_{\mathrm{act}}
+\lambda_{\mathrm{vl}}\mathcal L_{\mathrm{vl}}
+\lambda_{\mathrm{aux}}\mathcal L_{\mathrm{aux}}
$$

**图示、动画与 Notebook**

- 图示：VLM 接动作头；token/continuous action；多模态 batch。
- Notebook：小型 VLM backbone + continuous head；冻结与微调对比；输入消融。

**章节出口**

VLA 是接口家族而非单一模型，下一章比较不同动作头。

### 31　VLA 动作头：Token、Chunk、Diffusion 与 Flow Expert

**承接与目标**

比较不同动作表示和生成机制对精度、速度、多样性与控制频率的影响。

**知识点规划**

- action token、量化误差和交叉熵。
- 连续回归和异方差 Gaussian。
- ACT/CVAE action chunk。
- Diffusion action head。
- Flow Matching action expert。
- action query、Cross-Attention 和独立 expert。
- chunk、异步执行和 temporal ensemble。
- 多本体 action mask。
- 推理步数、连续性和安全过滤。

**核心公式**

$$
\mathcal L_{\mathrm{token}}=-\sum_i\log p_\theta(u_i\mid u_{<i},z_t)
$$

$$
\mathcal L_{\mathrm{flow}}
=\mathbb E\|v_\theta(A_\tau,\tau,z_t)-u_\tau\|^2
$$

**图示、动画与 Notebook**

- 动画：同一 Context 进入四种 head；token 量化、去噪、flow 积分。
- Notebook：统一 action head API；比较成功、平滑、延迟；安全限幅。

**章节出口**

单任务 VLA 已成立，但通用机器人需要大规模、异构、多任务数据。

## 单元二　通用机器人基础模型

### 32　多任务机器人策略与数据规模化

**承接与目标**

从一个任务一个模型，转向覆盖多场景、多指令和多技能的通用策略。

**知识点规划**

- single-task、multi-task 和 generalist policy。
- 数据规模、任务覆盖、模型容量和计算预算。
- heterogeneous dataset mixture。
- task balancing、temperature sampling 和 loss weighting。
- 语言重标注和任务标准化。
- pretraining、co-training、fine-tuning 和 adaptation。
- 数据质量、成功/失败和长尾任务。
- 数据量、场景、任务和本体多样性。
- 数据污染与 benchmark overfitting。

**核心公式**

$$
\mathcal L=\sum_{d=1}^{D}\lambda_d
\mathbb E_{\mathcal D_d}[\mathcal L_d(\theta)]
$$

$$
p_{\mathrm{sample}}(d)\propto n_d^\alpha
$$

**图示、动画与 Notebook**

- 图示：多数据集进入统一策略；规模三轴；采样权重。
- Notebook：混合两个动作尺度数据集；采样策略对比；分任务指标。

**章节出口**

多任务仍可能只适配同一种机器人，下一章处理跨本体接口。

### 33　跨任务、跨场景与跨本体学习

**承接与目标**

处理不同机器人在关节、相机、动作维度、动力学和工作空间上的差异。

**知识点规划**

- task、visual 和 embodiment generalization。
- 共享表示与本体专用控制。
- canonical、end-effector 与 native action space。
- embodiment token、adapter 和 mixture-of-experts。
- morphology、kinematics 和 control frequency。
- retargeting 与不可映射动作。
- human video 到 robot action 的 embodiment gap。
- few-shot adaptation 和 parameter-efficient tuning。

**核心公式**

$$
z_t^{\mathrm{shared}}=E(o_t,e),\qquad
a_t^{(e)}=D_{\mathrm{act}}^{(e)}(\pi_\theta(z_t^{\mathrm{shared}},g,e))
$$

**图示、动画与 Notebook**

- 图示：共享 backbone + 本体 adapter；动作重定向；工作空间差异。
- Notebook：两种二维机械臂共享表示；不同 action decoder；少样本适配。

**章节出口**

跨本体能力的瓶颈也在数据获取、清洗、标注和持续回流。

### 34　机器人数据引擎：示范、视频、仿真与自动标注

**承接与目标**

组合多种数据来源，形成采集—训练—部署—失败回流的持续改进闭环。

**知识点规划**

- 遥操作、示教、脚本策略和自主 rollout。
- 成功、失败、恢复和人工干预数据。
- 互联网视频、人类示范和无动作视频。
- latent action、inverse dynamics 和动作伪标签。
- 仿真、程序化场景和合成视频。
- 自动语言标注、任务分段和成功判定。
- 清洗、去重、同步、质量评分和版本化。
- active collection 与不确定性采样。
- policy rollout—人工纠正—重新训练。
- World Model 合成数据的物理一致性风险。

**核心公式**

$$
\mathcal D_{k+1}
=\mathcal D_k\cup\operatorname{Filter}(\operatorname{Collect}(\pi_k))
$$

$$
\theta_{k+1}=\operatorname{Train}(\mathcal D_{k+1})
$$

**图示、动画与 Notebook**

- 动画：数据飞轮；数据源监督矩阵；失败区域逐步覆盖。
- Notebook：episode 质量评分；任务阶段切分；ensemble 分歧驱动采集。

**章节出口**

数据引擎扩展策略，但动作模仿仍缺少显式未来预测，进入完整 World Model。

## 单元三　世界模型与 World-Action Model

### 35　世界模型：表示、动力学、观测与奖励

**承接与目标**

从系统角度定义一个可用于状态推断、想象和决策的完整 World Model。

**知识点规划**

- representation、transition 和 observation model。
- reward/cost 与 continue/termination model。
- inference state 与 imagined state。
- deterministic/stochastic、Markov/belief state。
- action-conditioned 与 goal-conditioned dynamics。
- training、filtering、rollout 和 planning 模式。
- World Model、模拟器、数字孪生和视频生成器的区别。
- 预测精度、决策充分性和成本。

**核心公式**

$$
p(o_{1:T},r_{1:T},z_{1:T}\mid a_{1:T})
=\prod_t p(z_t\mid z_{t-1},a_{t-1})p(o_t\mid z_t)p(r_t\mid z_t,a_t)
$$

**图示、动画与 Notebook**

- 图示：完整模块；观测更新与 imagined rollout；统一接口。
- Notebook：WorldModel API：encode、observe、imagine、decode、evaluate。

**章节出口**

完整接口建立后，需要比较不同世界模型范式及其适用目标。

### 36　视频世界模型、潜空间世界模型与预测式世界模型

**承接与目标**

区分生成视频、维护 belief 和预测抽象 embedding 三条路线。

**知识点规划**

- pixel generative world model。
- latent generative state-space model。
- joint-embedding predictive world model。
- object-centric 和 3D structured model。
- autoregressive、Diffusion/Flow video 和 latent transition。
- reconstruction、likelihood 与 predictive loss。
- 视觉逼真、物理一致、任务相关和规划可用。
- stochastic future 与多候选 rollout。
- inverse dynamics、state alignment 和 control-relevant representation。

**核心公式**

$$
\mathcal L_{\mathrm{pixel}}
=-\log p_\phi(o_{t+1:t+H}\mid o_{\leq t},A_t)
$$

$$
\mathcal L_{\mathrm{latent}}
=\sum_h\|P_\phi(z_t,A_{t:t+h-1})-{\rm sg}(z_{t+h})\|^2
$$

**图示、动画与 Notebook**

- 图示：三路线对照；视频逼真但动作响应错误；latent future 分叉。
- Notebook：同一数据训练 state/pixel/latent predictor；指标与规划成功对照。

**章节出口**

VLA 与 World Model 各有盲点，下一章研究二者如何协同。

### 37　VLA 与世界模型如何协同

**承接与目标**

让 VLA 提出动作，让 World Model 预测后果，并根据任务和风险选择。

**知识点规划**

- VLA proposal + World Model scoring。
- reranking、refinement 和 safety checking。
- policy prior + planning。
- World Model 合成策略训练数据。
- 真实 rollout 改进 World Model。
- shared/independent encoder 和 latent alignment。
- 高层子目标、技能和低层动作。
- episodic memory、失败记忆和检索。
- 主动感知：不确定时先重新观察。
- 快速直接行动与慢速想象的路由。

**核心公式**

$$
A^{(1:K)}\sim\pi_\theta(A\mid z_t,g)
$$

$$
k^*=\arg\max_k U(p_\phi(Z_{t+1:t+H}\mid z_t,A^{(k)}),g)
$$

**图示、动画与 Notebook**

- 动画：候选—rollout—评分；子目标—技能—动作；主动观测触发。
- Notebook：K 候选 rerank；直接 VLA/VLA+WM/MPC 对比；失败记忆。

**章节出口**

模块化协同仍可能重复编码、目标不一致，引出统一 World-Action Model。

### 38　统一 World-Action Model：理解、生成与预测

**承接与目标**

在共享表示或共享序列中联合学习“下一步做什么”和“做了以后怎样”。

**知识点规划**

- 独立模块、共享 backbone 和统一序列。
- action、future、inverse dynamics 联合目标。
- video-action joint generation。
- shared predictor 与独立 heads。
- action、visual、latent future token 的组织。
- 语义理解与物理动态的梯度冲突。
- loss balancing 和分阶段训练。
- action-only、world-only 和 joint rollout。
- 长程任务阶段与子目标预测。
- imagined data、preference/reward 与自我改进。

**核心公式**

$$
p_\Theta(A_{t:t+H},Z_{t+1:t+H}\mid o_{\leq t},g)
$$

$$
\mathcal L_{\mathrm{WAM}}
=\lambda_a\mathcal L_{\mathrm{action}}
+\lambda_w\mathcal L_{\mathrm{world}}
+\lambda_i\mathcal L_{\mathrm{inverse}}
+\lambda_r\mathcal L_{\mathrm{reward}}
$$

**图示、动画与 Notebook**

- 图示：模块化/共享/统一架构；联合 rollout；快速/慢速推理路径。
- Notebook：共享 encoder + action/dynamics heads；联合损失；两种推理模式。

**单元出口**

模型架构已经完整，最后处理新分布、仿真差距、实时约束和安全要求。

## 单元四　从模型走向真实机器人

### 42　泛化、分布偏移与鲁棒性

**承接与目标**

建立覆盖新物体、新背景、新指令、新任务、新动力学和新本体的评估视角。

**知识点规划**

- IID、OOD 和 domain shift。
- 视觉、语言、空间、任务、动力学和本体泛化轴。
- spurious correlation 和 shortcut learning。
- compositional generalization。
- augmentation、invariance 和 adaptation。
- uncertainty、calibration 和 failure prediction。
- robustness 与平均性能的区别。
- test-time adaptation 的风险。

**核心公式**

$$
R_{\mathrm{target}}(\pi)
=\mathbb E_{x\sim p_{\mathrm{target}}}[\ell(\pi(x),y)],
\qquad p_{\mathrm{target}}\neq p_{\mathrm{train}}
$$

**图示、动画与 Notebook**

- 图示：六种偏移矩阵；背景捷径；校准曲线。
- Notebook：颜色、背景、位置和语言扰动；分轴报告；置信度校准。

**章节出口**

数据增强只能缓解部分偏移，真机还存在外观、动力学和系统延迟差距。

### 43　仿真、数字孪生与 Sim-to-Real

**承接与目标**

利用仿真扩展训练与失败覆盖，同时明确跨越仿真—真实差距的方法和边界。

**知识点规划**

- simulator、digital twin 与 learned world model。
- visual、dynamics 和 latency gap。
- domain/dynamics randomization。
- system identification 和参数校准。
- sim-to-real、real-to-sim-to-real 和 residual learning。
- synthetic data、procedural generation 和 curriculum。
- parallel simulation 与 online RL。
- 真机少量校准和分层验证。

**核心公式**

$$
\xi\sim p(\xi),\qquad
\max_\theta\mathbb E_{\xi}[J(\pi_\theta;\mathcal M_\xi)]
$$

**图示、动画与 Notebook**

- 动画：仿真—真机差距；随机化参数空间；校准闭环。
- Notebook：随机化质量、摩擦、视觉和延迟；三级环境评估。

**章节出口**

算法能适应真机分布后，还必须满足实时推理和软硬件接口约束。

### 44　实时推理、异步执行与系统部署

**承接与目标**

把生成式策略和 World Model 放入不会无限等待的真实控制循环。

**知识点规划**

- sensing、preprocess、inference、communication 和 control latency。
- control、policy 和 action execution frequency。
- synchronous 与 asynchronous inference。
- chunk buffer、warm start 和 overlap。
- KV cache、token pruning 和多速率模块。
- quantization、distillation、compilation。
- edge 与 remote inference。
- watchdog、deadline miss 和 stale observation。
- 模型版本、日志和回放。

**核心公式**

$$
T_{\mathrm{loop}}
=T_{\mathrm{sense}}+T_{\mathrm{encode}}+T_{\mathrm{policy}}
+T_{\mathrm{world}}+T_{\mathrm{control}}
$$

$$
f_{\mathrm{loop}}\leq1/T_{\mathrm{loop}}
$$

**图示、动画与 Notebook**

- 动画：同步/异步时间线；latency budget；chunk 边界跳变。
- Notebook：模块计时；阻塞/缓存/异步对比；丢帧和动作队列检查。

**章节出口**

实时系统仍可能产生危险动作或进入异常状态，需要独立安全和恢复层。

### 45　安全约束、异常检测与失败恢复

**承接与目标**

在策略和 World Model 出错时，保证系统能够限制、停止、后退和重新规划。

**知识点规划**

- 硬件限位、速度/力矩和工作空间约束。
- collision checking。
- action filter、shield 和 control barrier function。
- uncertainty、OOD 和 anomaly detection。
- 感知、规划、控制、接触和系统故障分类。
- stop、hold、retreat、regrasp、reobserve 和 replan。
- human intervention 和 emergency stop。
- fail-safe/fail-operational。
- 失败日志和恢复数据回流。

**核心公式**

$$
a_t^{\mathrm{safe}}
=\arg\min_a\|a-a_t^{\mathrm{policy}}\|^2
\quad\text{s.t.}\quad C_i(s_t,a)\leq0
$$

$$
\dot h(s,a)+\alpha(h(s))\geq0
$$

**图示、动画与 Notebook**

- 图示：策略—安全过滤—控制器；失败恢复状态机；故障分层。
- Notebook：动作限幅、工作空间和碰撞约束；故障注入；failure log。

**章节出口**

具备安全机制后，需要覆盖感知、动作、预测、规划和部署的评测协议。

### 46　机器人策略与世界模型的系统评测

**承接与目标**

避免只用单个 loss、视频质量或平均成功率判断完整系统。

**知识点规划**

- component、offline 和 closed-loop metric。
- 感知：grounding、pose、retrieval 和 calibration。
- 动作：误差、平滑、diversity、latency 和 chunk consistency。
- World Model：单步、多步、物理一致和 planning utility。
- 策略：成功、进度、碰撞、恢复和时间。
- 泛化矩阵与 cross-embodiment 评估。
- confidence interval、随机种子和统计显著性。
- ablation、failure taxonomy 和 replay。
- benchmark leakage 和公平比较。

**核心公式**

$$
\hat p=\frac{1}{N}\sum_{i=1}^{N}\mathbf1[\mathrm{success}_i]
$$

$$
\operatorname{ECE}
=\sum_m\frac{|B_m|}{N}
|\operatorname{acc}(B_m)-\operatorname{conf}(B_m)|
$$

**图示、动画与 Notebook**

- 图示：模块到系统指标树；泛化矩阵；失败根因追踪。
- Notebook：episode evaluator；bootstrap 置信区间；失败时间线。

**章节出口**

所有模块和评测接口已经具备，最后连接成最小但完整的闭环系统。

### 47　最终系统：可预测、可规划、可纠错的闭环机器人

**承接与目标**

把分散章节连接起来，让读者能追踪每个张量、模型和安全检查。

**知识点规划**

- sensor adapter 与 observation contract。
- vision/language/proprio/tactile encoders。
- multimodal Context Model。
- VLA/Action Model 候选动作。
- World Model 候选未来。
- goal/reward/value/critic。
- planner 的选择、refine 和拒绝。
- safety shield 与底层控制。
- belief 更新、偏差检测和重新规划。
- memory、failure log 和 data flywheel。
- 快速反应路径与慢速想象路径。
- 模块替换、消融和最小成功标准。

**核心公式**

$$
z_t=E(o_{\leq t},g)
$$

$$
A_t^{(1:K)}\sim\pi_\theta(A\mid z_t)
$$

$$
\hat Z^{(k)}\sim p_\phi(Z\mid z_t,A_t^{(k)})
$$

$$
k^*=\arg\max_k
\left[U(\hat Z^{(k)},g)-\lambda_{\mathrm{risk}}R(\hat Z^{(k)},A_t^{(k)})\right]
$$

$$
a_t=\operatorname{SafetyFilter}(A_{t,0}^{(k^*)},o_t)
$$

**最终架构**

~~~mermaid
flowchart LR
    O["图像 / 语言 / 本体 / 触觉"] --> C["Context Model"]
    C --> P["VLA / Action Model"]
    P --> A["K 条候选动作"]
    A --> W["World Model"]
    W --> F["候选未来"]
    F --> U["Goal / Reward / Value"]
    U --> S["Planner 选择"]
    S --> G["Safety Filter"]
    G --> R["机器人执行"]
    R --> N["新观测与失败检测"]
    N --> C
~~~

**图示、动画与 Notebook**

- 动画：完整闭环逐模块点亮；正常执行、预测偏差和失败恢复。
- examples/final-system 实现统一接口和 toy environment。
- 至少生成两条候选动作并比较预测后果。
- 注入执行扰动，要求重新观测和规划。
- 记录观测、latent、候选、预测、评分、动作和安全决策。
- 消融：无历史、无 World Model、无 Planner、无 Safety。

**最终完成标准**

- 能从一条 episode 追踪完整数据流。
- 能说明 VLA、World Model、Planner 和 Safety 各自回答什么问题。
- 能在至少一种扰动下闭环纠错。
- 能用系统指标和失败日志解释成功或失败。
- 能替换一个模块而不破坏其余接口。

---

# 五、分阶段实践项目

| 阶段 | 项目 | 对应章节 | 最小产物 |
|---|---|---|---|
| 基础篇 | 多模态反应式策略 | 03—11 | 图像/目标/本体输入，输出单步动作并闭环执行 |
| 进阶篇 | 生成式动作策略 | 12—20 | 生成并滚动执行多峰 Action Chunk |
| 进阶篇 | 动作条件世界预测 | 21—27 | 预测候选未来并使用 MPC/CEM 选择动作 |
| 高级篇 | Mini-VLA | 28—34 | 语言条件、多任务视觉动作模型 |
| 高级篇 | Mini World-Action System | 35—47 | 候选动作、未来预测、评价、安全与恢复闭环 |

# 六、数学知识的 Just-in-Time 路线

| 数学主题 | 首次正式出现 |
|---|---|
| 向量、矩阵、张量和导数 | 01—02 |
| 卷积与空间尺寸 | 03 |
| 投影、齐次变换与 \(SE(3)\) | 04 |
| 概率分布与 KL 散度 | 07 |
| 相似度、Softmax 与交叉熵 | 08 |
| 条件概率与最大似然 | 10、17 |
| 递推、卷积序列与状态空间 | 12 |
| 矩阵注意力 | 13 |
| 随机过程与去噪 | 18 |
| 常微分方程与向量场 | 19 |
| 隐变量状态空间模型 | 23 |
| 方差分解与校准 | 24 |
| Bellman 方程与期望回报 | 25、27 |
| 采样优化与约束优化 | 26、45 |

# 七、章节验收清单

- 是否明确回答一个承接上一章的问题。
- 是否说明输入、输出、张量形状和物理语义。
- 是否区分训练、推理和真实执行。
- 是否至少给出一个核心公式并解释符号。
- 是否包含一个机器人场景具体例子。
- 是否包含一个失败案例或适用边界。
- 是否有静态图；需要动态表达时是否提供动画占位。
- 是否有可运行代码、Notebook，或明确说明本章仅讲接口。
- 是否避免虚构实验数据和未经核对的论文结论。
- 是否留下下一章必须解决的矛盾。
- 是否通过 Markdown、链接、公式与网页显示检查。
- 代码是否能从干净环境运行，并记录依赖与随机种子。

# 八、最终知识地图

~~~mermaid
flowchart TB
    A["01—02<br/>问题、数据与学习"] --> B["03—06<br/>视觉、几何、语言、本体与动作"]
    B --> C["07—09<br/>重建、对齐、预测与融合"]
    C --> D["10—11<br/>行为克隆与第一个闭环"]
    D --> E["12—15<br/>时间、Attention、Transformer 与 Context"]
    E --> F["16—20<br/>动作块与生成式 Action Model"]
    F --> G["21—24<br/>动力学与 World Model"]
    G --> H["25—27<br/>评价、规划与策略改进"]
    H --> I["28—31<br/>VLM、Grounding 与 VLA"]
    I --> J["32—34<br/>通用策略、跨本体与数据引擎"]
    J --> K["35—38<br/>World Model 与 World-Action Model"]
    K --> L["42—47<br/>泛化、仿真、部署、安全与最终系统"]
~~~
